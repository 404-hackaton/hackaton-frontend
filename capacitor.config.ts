import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.github.404-hackaton.404-frontend-ionic',
  appName: 'EduApp',
  webDir: 'dist'
};

export default config;
